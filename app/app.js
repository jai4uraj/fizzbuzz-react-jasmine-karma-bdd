import React, {Component} from 'react';
import ReactDOM from 'react-dom';
import Index from '../Components/index';

var obj = React.createElement(Index, {});
ReactDOM.render(obj, document.getElementById('FizzBuzzJasminKarma'));